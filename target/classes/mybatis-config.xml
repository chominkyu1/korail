<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
          "http://mybatis.org/dtd/mybatis-3-config.dtd">       
<!-- mybatis-config.xml -->
<configuration>
    <typeAliases>
    
       <!-- 매퍼XML(board.xml)안에서 parameterType="board"
                                  resultType="board" -->
       <typeAlias type="com.encore.domain.BoardVO" alias="board"/>
       
       <!-- 매퍼XML(board.xml)안에서 parameterType="BoardVO"
                                  resultType="BoardVO" -->
       <!-- <package name="com.encore.domain"/> -->
       <package name="com.encore.domain"/>
    </typeAliases>
</configuration>     